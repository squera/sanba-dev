[package]
name = "stream"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = "0.5.1"
rocket_cors = "0.5.2"
tokio = { version = "1.0", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

